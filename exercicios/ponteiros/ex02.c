#include <stdio.h>
#include <string.h>

void reverse(char *string, int size){
    char temp[size];
    for (int i = size - 1; )
}

int main(){
    char string[50];

    printf("input a string: ");
    fgets(string, 50, stdin);

    return 0;
}